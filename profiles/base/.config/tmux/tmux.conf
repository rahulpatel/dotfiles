# Configuration
bind r source-file ~/.config/tmux/tmux.conf \; display-message 'Configuration reloaded'

# Vi mode for copy
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# Pane controls
bind h split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"
bind x kill-pane

bind -n C-M-Left select-pane -L
bind -n C-M-Right select-pane -R
bind -n C-M-Up select-pane -U
bind -n C-M-Down select-pane -D

bind -n C-M-S-Left resize-pane -L 5
bind -n C-M-S-Right resize-pane -D 5
bind -n C-M-S-Up resize-pane -U 5
bind -n C-M-S-Down resize-pane -R 5

# Window navigation
bind c new-window -c "#{pane_current_path}"
bind k kill-window

bind -n C-S-Left select-window -t -1
bind -n C-S-Right select-window -t +1
bind -n C-M-S-Left swap-window -t -1 \; select-window -t -1
bind -n C-M-S-Right swap-window -t +1 \; select-window -t +1

# Session controls
bind P switch-client -p
bind N switch-client -n

# General
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",*:RGB"
set -g mouse on
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g history-limit 50000
set -g escape-time 0
set -g focus-events on
set -g set-clipboard on
set -g allow-passthrough on
setw -g aggressive-resize on
set -g detach-on-destroy off
set -g extended-keys on
set -g extended-keys-format csi-u
set -sg escape-time 10

# Status bar
set -g status-position top
set -g status-interval 1
set -g status-left-length 30
set -g status-right-length 50
set -g window-status-separator ""

# Theme
set -g status-style "bg=default,fg=default"
set -g status-left-length 100
set -g status-left "#[fg=black,bg=default] #S #[fg=#fab387]#{?pane_in_mode,COPY,}#{?client_prefix,PREFIX,}#{?window_zoomed_flag,ZOOM,} "
set -g status-right "#[fg=black]%H:%M"
set -g window-status-format " #(~/.config/tmux/bin/window-icon #{pane_current_command}) #{pane_current_command} "
set -g window-status-current-format "#[fg=#fab387] #(~/.config/tmux/bin/window-icon #{pane_current_command}) #{pane_current_command} "
set -g window-status-separator "#[fg=black,bg=default] • "
set -g status-justify absolute-centre
set -g pane-border-style "fg=brightblack"
set -g pane-active-border-style "fg=blue"
set -g message-style "bg=default,fg=#fab387"
set -g message-command-style "bg=default,fg=#fab387"
